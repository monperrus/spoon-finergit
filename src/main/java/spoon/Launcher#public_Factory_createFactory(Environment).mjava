/** 
 * Creates a default factory with the given environment.
 * @param environment the factory's environment
 * @return the created factory
 */
public
Factory
createFactory
(
Environment
environment
)
{
return
new
Factory
(
new
DefaultCoreFactory
(
)
,
environment
)
;
}
