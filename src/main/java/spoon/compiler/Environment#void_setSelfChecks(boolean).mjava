/** 
 * Enable or not consistency checks on the AST. See  {@link #checksAreSkipped()} for a list of all checks.
 * @param skip false means that all checks are made (default), true means that no checks are made.Use  {@link #disableConsistencyChecks()} instead.
 */
@Deprecated
void
setSelfChecks
(
boolean
skip
)
;
