/** 
 * Builds the program's model with the given factory and stores the result into this factory. Note that this method should only be used once on a given factory.
 * @return true if the Java was successfully compiled with the core Javacompiler, false if some errors were encountered while compiling
 * @exception Exception when a building problem occurs
 */
boolean
build
(
)
throws
Exception
;
