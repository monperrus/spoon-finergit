/** 
 * Builds the program's model corresponding to the given files and stores the result into its factory ( {@link #getFactory()}). Note that this method should only be used once on a given factory.
 * @return true if the model was successfully built
 * @exception Exception when a building problem occurs
 * @see #getSourceClasspath()
 */
boolean
build
(
List
<
SpoonFile
>
files
)
throws
Exception
;
