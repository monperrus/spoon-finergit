/** 
 * Builds the program's model corresponding to the given files stores the result into the template factory (see  {@link Factory#Template()}. Note that this method should only be used once on a given factory.
 * @return true if the model was successfully built
 * @exception Exception when a building problem occurs
 * @see #getTemplateClasspath()
 * @see Factory#Template()
 */
boolean
buildTemplates
(
List
<
SpoonFile
>
files
)
throws
Exception
;
