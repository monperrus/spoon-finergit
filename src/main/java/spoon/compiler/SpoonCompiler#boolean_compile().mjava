/** 
 * Generates the bytecode associated to the classes stored in the given factory. The bytecode is generated in the directory given by {@link #getDestinationDirectory()}.
 */
boolean
compile
(
)
;
