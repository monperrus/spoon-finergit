/** 
 * Tells if this element contains the given modifier.
 * @param modifier to search
 * @return {@code true} if this element contain the modifier
 */
boolean
hasModifier
(
ModifierKind
modifier
)
;
