/** 
 * Extract the variable from a string
 */
String
extractVariable
(
String
value
)
{
String
val
=
value
;
if
(
value
!=
null
&&
value
.
contains
(
"$"
)
)
{
Matcher
matcher
=
mavenProperty
.
matcher
(
value
)
;
while
(
matcher
.
find
(
)
)
{
String
var
=
matcher
.
group
(
)
;
val
=
val
.
replace
(
var
,
getProperty
(
var
.
substring
(
2
,
var
.
length
(
)
-
1
)
)
)
;
}
}
return
val
;
}
