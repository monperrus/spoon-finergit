/** 
 * @return true if this matcher can be applied more than once in the same container of targetsNote: even if false, it may be applied again to another container and to match EQUAL value
 */
public
boolean
isRepeatable
(
)
{
if
(
repeatable
!=
null
)
{
return
repeatable
;
}
return
isMultiple
(
)
;
}
