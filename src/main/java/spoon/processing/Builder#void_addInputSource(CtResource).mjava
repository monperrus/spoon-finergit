/** 
 * Adds a file/directory (as a CtResource) to be built. By default, the files could be Java source files or Jar files. Directories are processed recursively.
 * @param source file or directory to add
 */
void
addInputSource
(
CtResource
source
)
throws
IOException
;
