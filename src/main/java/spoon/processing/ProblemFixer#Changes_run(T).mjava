/** 
 * Runs this fix on given element. This fixer should modify the given model and return a list of the modified elements.
 * @param element the element marked by a problem
 * @return List of modified elements
 */
Changes
run
(
T
element
)
;
