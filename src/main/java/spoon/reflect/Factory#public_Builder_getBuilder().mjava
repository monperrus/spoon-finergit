/** 
 * Returns a builder for this factory (creates it if not existing yet).
 */
public
Builder
getBuilder
(
)
{
if
(
builder
==
null
)
builder
=
new
SpoonBuildingManager
(
this
)
;
return
builder
;
}
