/** 
 * Returns the parent of this factory. When an element is not found in a factory, it can be looked up in its parent factory using a delegation model.
 */
public
Factory
getParentFactory
(
)
{
return
parentFactory
;
}
