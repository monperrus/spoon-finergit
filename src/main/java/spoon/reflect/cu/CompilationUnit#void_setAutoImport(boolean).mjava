/** 
 * Sets this compilation unit auto-import feature on or off (default is on). Note that setting off auto-import is not recommended as it can lead to unresolved references in the generated code.
 * @see #getManualImports()
 * @see #setManualImports(Set)
 */
void
setAutoImport
(
boolean
autoImport
)
;
