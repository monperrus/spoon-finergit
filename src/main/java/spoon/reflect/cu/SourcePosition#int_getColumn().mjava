/** 
 * Gets the column in the source file (1 indexed). This method is slow because it has to calculate the column number depending on {@link Environment#getTabulationSize()} and{@link CompilationUnit#getOriginalSourceCode()}. Prefer  {@link #getSourceStart()}.
 */
int
getColumn
(
)
;
