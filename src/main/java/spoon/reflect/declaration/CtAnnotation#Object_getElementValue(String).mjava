/** 
 * Gets a value for a given key (with conversion if needed). <p> NOTE: in case of a type, the value is converted to the actual type. To access the type as a reference, use  {@link #getElementValues()}, which returns a map containing the raw (unconverted) values.
 * @param key name of searched value
 * @return the value or null if not found
 */
Object
getElementValue
(
String
key
)
;
