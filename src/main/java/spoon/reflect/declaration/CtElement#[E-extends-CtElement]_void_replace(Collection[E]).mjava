/** 
 * Replaces this element by several elements. If `elements` contains one single element, it is equivalent to  {@link #replace(CtElement)}. If `elements` is empty, it is equivalent to  {@link #delete()}.
 */
<
E
extends
CtElement
>
void
replace
(
Collection
<
E
>
elements
)
;
