/** 
 * Searches a child package by name.
 * @param name the simple name of searched package
 * @return the found package or null
 */
@PropertyGetter(role=SUB_PACKAGE)
CtPackage
getPackage
(
String
name
)
;
