/** 
 * Return all the accessible methods (concrete and abstract) for this type. The recursion stops when the super-type/super-interface is not in the model.
 */
Set
<
CtMethod
<
?
>
>
getAllMethods
(
)
;
