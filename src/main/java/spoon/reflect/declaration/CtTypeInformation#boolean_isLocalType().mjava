/** 
 * Return  {@code true} if the referenced type is declared in an executable.e.g. a type declared in a method or a lambda. This corresponds to <code>isLocalClass</code> of <code>java.lang.Class</code>. <pre> // Type declared in a method. public void make() { class Cook { } } // Type declared in a lambda. s -&gt; { class Cook { } } </pre>
 */
boolean
isLocalType
(
)
;
