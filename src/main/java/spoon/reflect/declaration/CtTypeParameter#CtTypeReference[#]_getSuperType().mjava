/** 
 * A type parameter can have an <code>extends</code> clause which declare one ( {@link CtTypeReference} or more ({@link CtIntersectionTypeReference} references.
 */
CtTypeReference
<
?
>
getSuperType
(
)
;
