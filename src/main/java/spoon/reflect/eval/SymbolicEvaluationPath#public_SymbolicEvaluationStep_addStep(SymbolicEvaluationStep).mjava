/** 
 * Creates and adds a step to this path.
 * @param kind the step kind
 * @param evaluator the current evaluator (its state is used to initialize the new step).
 * @return the new SymbolicEvaluationStep
 */
public
SymbolicEvaluationStep
addStep
(
SymbolicEvaluationStep
step
)
{
steps
.
add
(
step
)
;
return
step
;
}
