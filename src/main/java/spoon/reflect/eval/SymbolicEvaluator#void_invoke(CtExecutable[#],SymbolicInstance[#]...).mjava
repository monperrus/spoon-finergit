/** 
 * Starts a symbolic evaluation by invoking a given static executable. Note that symbolic evaluation may have several paths and thus several results. In order to access the evaluation information, you need to use {@link #addObserver(SymbolicEvaluatorObserver)}.
 * @param executable to be invoked symbolically
 * @param args the arguments of the call (as symbolic instances)
 */
void
invoke
(
CtExecutable
<
?
>
executable
,
SymbolicInstance
<
?
>
...
args
)
;
