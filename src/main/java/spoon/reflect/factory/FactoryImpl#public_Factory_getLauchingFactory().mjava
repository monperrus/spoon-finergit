/** 
 * Gets the factory that was created at launching time (the firstly created factory). This factory is automatically initialized when a factory is contructed for the first time. Any subsequent constructions will not affect the launching factory and references to other factories have to be handled manually, if ever needed.
 */
public
static
Factory
getLauchingFactory
(
)
{
return
launchingFactory
;
}
