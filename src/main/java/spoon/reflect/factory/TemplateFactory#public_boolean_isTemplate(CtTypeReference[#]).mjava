/** 
 * Helper method to know if a given type reference points to a Template or not
 * @param candidate the reference to check
 * @return <code> true </code> if the reference points to a template type
 */
public
boolean
isTemplate
(
CtTypeReference
<
?
>
candidate
)
{
return
templates
.
containsKey
(
candidate
.
getQualifiedName
(
)
)
;
}
