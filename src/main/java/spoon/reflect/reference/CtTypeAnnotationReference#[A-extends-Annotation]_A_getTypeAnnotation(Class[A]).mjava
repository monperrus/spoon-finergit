/** 
 * Searches for an type annotation of the given class that annotates the current type.
 * @param < A > the annotation's type
 * @param annotationType the annotation's class
 * @return if found, returns an Annotation
 */
<
A
extends
Annotation
>
A
getTypeAnnotation
(
Class
<
A
>
annotationType
)
;
