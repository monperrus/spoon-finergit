/** 
 * Searches for an type annotation of the given type reference that annotates the current type.
 * @param < A > the annotation's type
 * @param annotationType the annotation's type
 * @return if found, returns an Annotation
 */
<
A
extends
Annotation
>
CtAnnotation
<
A
>
getTypeAnnotation
(
CtTypeReference
<
A
>
annotationType
)
;
