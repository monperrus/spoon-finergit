/** 
 * A type parameter can have an <code>extends</code> clause which declare one ( {@link CtTypeReference} or more ({@link CtIntersectionTypeReference} references.<pre> // Extends with generics. T extends Interface1 // Intersection type with generics. T extends Interface1 &amp; Interface2 </pre>
 */
@PropertyGetter(role=CtRole.BOUNDING_TYPE)
CtTypeReference
<
?
>
getBoundingType
(
)
;
