/** 
 * If this type reference is used as a type argument (see  {@link #getActualTypeArguments()}), returns the type parameter declaration in the target type, returns null otherwise. In the following example, getTypeParameterDeclaration of "String" returns the type parameter definition "X". <pre> class Dog&lt;X&gt;{} Dog&lt;String&gt;var = ...; </pre> In this other example, getTypeParameterDeclaration of T in Dog&lt;T&gt; returns the type parameter definition "X" (while  {@link #getDeclaration()} returns the "T" of Cat).<pre> class Dog&lt;X&gt;{} class Cat&lt;T&gt; { Dog&lt;T&gt; dog; } </pre>
 */
@DerivedProperty
CtTypeParameter
getTypeParameterDeclaration
(
)
;
