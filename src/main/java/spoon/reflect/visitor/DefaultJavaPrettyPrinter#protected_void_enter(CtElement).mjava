/** 
 * This method is called by  {@link #scan(CtElement)} when entering a scanned element.To be overridden to implement specific behavior. Same KISS design as for  {@link CtScanner}.
 */
protected
void
enter
(
CtElement
e
)
{
}
