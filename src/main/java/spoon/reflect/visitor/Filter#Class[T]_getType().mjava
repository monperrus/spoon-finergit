/** 
 * Gets the runtime type that corresponds to the <code>T</code> parameter (the type of the filtered elements). Any element assignable from this type is a potential match and is tested using the {@link #matches(CtElement)} method, while other elements are never amatch.
 */
Class
<
T
>
getType
(
)
;
