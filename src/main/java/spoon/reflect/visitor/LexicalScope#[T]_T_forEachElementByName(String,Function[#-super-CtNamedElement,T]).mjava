/** 
 * @param name to be searched simple name
 * @param fnc is called for each named element with same simple name, which is defined in this or parent {@link LexicalScope}. Function `fnc` is called as long as there are some matching elements and `fnc` returns null. If `fnc` returns not null value then searching is stopped and that value is a returned
 * @return the value returned by `fnc` or null
 */
<
T
>
T
forEachElementByName
(
String
name
,
Function
<
?
CtNamedElement
,
T
>
fnc
)
;
