/** 
 * @return {@link LexicalScope} of actually scanned element. The {@link LexicalScope#forEachElementByName(String,java.util.function.Function)} can be usedto get all  {@link CtElement}s which are mapped to that simple name
 */
public
LexicalScope
getCurrentLexicalScope
(
)
{
LexicalScope
ns
=
scopes
.
peek
(
)
;
return
ns
==
null
?
EMPTY
:
ns
;
}
