/** 
 * Within a given factory, returns all the program elements that match the filter.
 * @param < E > the type of the sought program elements
 * @param factory the factory that contains the elements where to recursive search on
 * @param filter the filter which defines the matching criteria
 */
public
static
<
E
extends
CtElement
>
List
<
E
>
getElements
(
Factory
factory
,
Filter
<
E
>
filter
)
{
return
getElements
(
factory
.
Package
(
)
.
getRootPackage
(
)
,
filter
)
;
}
