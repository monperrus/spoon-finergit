/** 
 * Tells if the processing is stopped, generally because one of the processors called  {@link #setProcessingStopped(boolean)} after reportingan error.
 */
@Override
public
boolean
isProcessingStopped
(
)
{
return
processingStopped
;
}
