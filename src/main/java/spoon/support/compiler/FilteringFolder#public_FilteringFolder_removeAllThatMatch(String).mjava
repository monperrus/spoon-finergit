/** 
 * Removes all resources matching the given Java regex Eg. resources3.removeIfMatches(".*packageprotected.*");
 */
public
FilteringFolder
removeAllThatMatch
(
String
regex
)
{
for
(
SpoonResource
f
:
new
ArrayList
<
>
(
files
)
)
{
if
(
f
.
getPath
(
)
.
matches
(
regex
)
)
{
files
.
remove
(
f
)
;
}
}
return
this
;
}
