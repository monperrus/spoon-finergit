/** 
 * We have a source fragment of to be printed element. Print unmodified parts of this source `fragment`
 * @param fragment
 * @param isModified true if at least some part of `SourceFragment` is modified.false if whole `SourceFragment` including all children is not modified.
 */
void
printSourceFragment
(
SourceFragment
fragment
,
Boolean
isModified
)
;
