/** 
 * @return true if this printer is able to handle this eventThat is that we can safely call  {@link #print(PrinterEvent)} afterhaving called this one.
 */
boolean
knowsHowToPrint
(
PrinterEvent
event
)
;
