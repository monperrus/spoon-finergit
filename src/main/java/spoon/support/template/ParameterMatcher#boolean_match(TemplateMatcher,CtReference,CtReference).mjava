/** 
 * To be defined to implement a matching strategy for template parameter(s).
 * @param templateMatcher the instance of the matcher that is currently performing the matching (up-caller)
 * @param template the template reference to match against
 * @param toMatch the reference to be tested for a match
 * @return true if matching
 */
boolean
match
(
TemplateMatcher
templateMatcher
,
CtReference
template
,
CtReference
toMatch
)
;
