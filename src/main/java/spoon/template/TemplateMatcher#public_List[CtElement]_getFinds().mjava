/** 
 * Returns all the elements that correspond to a given template. The {@link #find(CtElement,CtElement)} method must have been called before
 */
public
List
<
CtElement
>
getFinds
(
)
{
return
finds
;
}
