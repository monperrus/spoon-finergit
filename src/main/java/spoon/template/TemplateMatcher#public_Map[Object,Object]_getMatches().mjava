/** 
 * Returns all the matches in a map where the keys are the corresponding template parameters. The  {@link #match(CtElement,CtElement)} method musthave been called before.
 */
public
Map
<
Object
,
Object
>
getMatches
(
)
{
return
matches
;
}
