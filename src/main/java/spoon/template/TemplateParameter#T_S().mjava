/** 
 * Gets the type of the template parameter. This methods has no runtime meaning (should return a <code>null</code> reference) but is used as a marker in a template code. When generating a template code, each invocation of this method will be substituted with the result of the {@link #getSubstitution(CtSimpleType)} method.
 */
T
S
(
)
;
