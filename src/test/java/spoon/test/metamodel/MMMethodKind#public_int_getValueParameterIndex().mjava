/** 
 * @return index of parameter, which contains the field value. idx >= 0 - for method parameters  idx = -1 - for return value of the method idx = -2 - unknown
 */
public
int
getValueParameterIndex
(
)
{
return
valueParameterIndex
;
}
