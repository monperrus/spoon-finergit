@Test
public
void
testJava12SwitchExpressionInIf
(
)
{
String
code
=
"class A { public void f(int i) { if (switch (i) { case 1, 2 -> true; default -> false; }) {} }; } }"
;
Launcher
launcher
=
new
Launcher
(
)
;
launcher
.
getEnvironment
(
)
.
setComplianceLevel
(
12
)
;
launcher
.
getEnvironment
(
)
.
setPreviewFeaturesEnabled
(
true
)
;
launcher
.
getEnvironment
(
)
.
setNoClasspath
(
true
)
;
launcher
.
addInputResource
(
new
VirtualFile
(
code
)
)
;
CtModel
model
=
launcher
.
buildModel
(
)
;
CtIf
ctIf
=
model
.
getElements
(
new
TypeFilter
<
>
(
CtIf
.
class
)
)
.
get
(
0
)
;
assertEquals
(
"if (switch (i) {    case 1, 2 ->        true;    default ->        false;}) {}"
,
toSingleLineString
(
ctIf
)
)
;
}
