/** 
 * Parses a ppt parent hierarchy record and returns it. 
 */
private
static
ParentRelation
parse_ppt_parent
(
ParseState
state
,
Scanner
scanner
)
throws
DeclError
{
ParentRelation
pr
=
new
ParentRelation
(
)
;
pr
.
rel_type
=
parse_enum_val
(
state
,
scanner
,
PptRelationType
.
class
,
"relation type"
)
;
pr
.
parent_ppt_name
=
need
(
state
,
scanner
,
"ppt name"
)
;
pr
.
id
=
Integer
.
parseInt
(
need
(
state
,
scanner
,
"relation id"
)
)
;
need_eol
(
state
,
scanner
)
;
return
(
pr
)
;
}
