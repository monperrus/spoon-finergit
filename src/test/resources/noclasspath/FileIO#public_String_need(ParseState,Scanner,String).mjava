/** 
 * Helper function, returns the next string token unescaped and interned.  Throw a DeclError if there is no next token
 */
public
static
String
need
(
ParseState
state
,
Scanner
scanner
,
String
description
)
throws
DeclError
{
if
(
!
scanner
.
hasNext
(
)
)
decl_error
(
state
,
"end-of-line found where %s expected"
,
description
)
;
return
unescape_decl
(
scanner
.
next
(
)
)
.
intern
(
)
;
}
