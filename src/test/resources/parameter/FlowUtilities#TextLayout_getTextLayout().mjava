/** 
 * Provides a TextLayout that can be used by the Draw2d text package for Bidi.  This  TextLayout should not be disposed by clients.  The provided TextLayout's orientation will be LTR.
 * @return an SWT TextLayout that can be used for Bidi
 * @since 3.1
 */
static
TextLayout
getTextLayout
(
)
{
if
(
layout
==
null
)
layout
=
new
TextLayout
(
Display
.
getDefault
(
)
)
;
layout
.
setOrientation
(
SWT
.
LEFT_TO_RIGHT
)
;
return
layout
;
}
